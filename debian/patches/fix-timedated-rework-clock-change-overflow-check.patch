From fff6dfc75e4a2eabff2dea61d77a74cfdb949b66 Mon Sep 17 00:00:00 2001
From: Lennart Poettering <lennart@poettering.net>
Date: Tue, 18 Feb 2025 09:37:41 +0100
Subject: [PATCH] timedated: rework clock change overflow check

Let's use explicit language constructs to test for overflow, the
previous code was ultimately relying on undefined behaviour.

diff --git a/src/timedate/timedated.c b/src/timedate/timedated.c
index b196034a25..6ee49c5975 100644
--- a/src/timedate/timedated.c
+++ b/src/timedate/timedated.c
@@ -877,16 +877,16 @@ static int method_set_time(sd_bus_message *m, void *userdata, sd_bus_error *erro
                 return sd_bus_reply_method_return(m, NULL);
 
         if (relative) {
-                usec_t n, x;
-
-                n = now(CLOCK_REALTIME);
-                x = n + utc;
-
-                if ((utc > 0 && x < n) ||
-                    (utc < 0 && x > n))
+                usec_t n = now(CLOCK_REALTIME);
+                bool valid;
+                if (utc > 0)
+                        valid = INC_SAFE(&n, (usec_t) utc);
+                else
+                        valid = DEC_SAFE(&n, (usec_t) -utc);
+                if (!valid)
                         return sd_bus_error_set(error, SD_BUS_ERROR_INVALID_ARGS, "Time value overflow");
 
-                timespec_store(&ts, x);
+                timespec_store(&ts, n);
         } else
                 timespec_store(&ts, (usec_t) utc);
 
@@ -909,7 +909,7 @@ static int method_set_time(sd_bus_message *m, void *userdata, sd_bus_error *erro
         if (r < 0 && r != -ENODATA)
                 return r;
 
-        timespec_store(&ts, timespec_load(&ts) + (now(CLOCK_MONOTONIC) - start));
+        timespec_store(&ts, timespec_load(&ts) + usec_sub_unsigned(now(CLOCK_MONOTONIC), start));
 
         /* Set system clock */
         if (clock_settime(CLOCK_REALTIME, &ts) < 0) {


diff --git a/src/basic/time-util.h b/src/basic/time-util.h
--- a/src/basic/time-util.h
+++ b/src/basic/time-util.h
@@ -191,6 +191,20 @@ static inline usec_t usec_add(usec_t a, usec_t b) {
         return a + b;
 }
 
+static inline bool INC_SAFE(uint64_t *p, uint64_t add) {
+        if (*p > UINT64_MAX - add)
+                return false;
+        *p += add;
+        return true;
+}
+
+static inline bool DEC_SAFE(uint64_t *p, uint64_t sub) {
+        if (*p < sub)
+                return false;
+        *p -= sub;
+        return true;
+}
+
 static inline usec_t usec_sub_unsigned(usec_t timestamp, usec_t delta) {
         if (timestamp == USEC_INFINITY) /* Make sure infinity doesn't degrade */
                 return USEC_INFINITY;
